# Copy to `.env.local`. URL of the deployed Worker API.
# For local dev this is the wrangler dev server:
VITE_API_URL="http://localhost:8787"
# In production, your deployed worker, e.g.:
# VITE_API_URL="https://email-client-api.<your-subdomain>.workers.dev"
